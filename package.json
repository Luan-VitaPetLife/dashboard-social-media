{
  "name": "dashboard-social-media",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Dashboard de métricas de redes sociais (Coco and Luna) — seguidores, curtidas e engajamento via Meta Graph API.",
  "scripts": {
    "start": "node server.js",
    "sync": "node -e \"import('./src/sync.js').then(m => m.runSync())\""
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2",
    "dotenv": "^16.4.5",
    "mongodb": "^6.9.0"
  }
}
